<%= form_for(ocupacion) do |f| %>
  <% if ocupacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ocupacion.errors.count, "error") %> prohibited this ocupacion from being saved:</h2>

      <ul>
      <% ocupacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
