<div class="ibox float-e-margins">
              <div class="ibox-title">
                  <h2 class="parametros-titulo"><%= "Rol" %></h2>
              </div>
          </div>
<div class="ibox-content">
<%= form_for @rol, :html => {:class => 'form-horizontal'} do |f| %>          
  <% if rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rol.errors.count, "error") %> prohibited this rol from being saved:</h2>

      <ul>
      <% rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


                 <div class="form-group">
                    <label class="col-lg-2 control-label">Descripción</label>
                    <div class="col-lg-3"><%= f.text_field :descripcion, :class => "form-control" %>
                    </div>
                    </div> 

<%= f.label :Opciones_Disponibles %>

  <div class="container-fluid">
    <% @options.each do |opcion| %>
      <div class="col-sm-3">
          <%= check_box_tag('option_menus[]', opcion.id, @rol.option_menu.include?(opcion)) %> <%= opcion.nombre %>
      </div>
    <% end %>
  </div> 


   <div class="form-group">
      <div class="col-lg-offset-3 col-lg-2">
           <%= f.submit "Guardar", :class => "btn btn-sm btn-primary" %>
      </div>
      <% if (@rol.new_record?) %>
        <div class="col-lg-2">
          <%= link_to 'Limpiar', '', type: :reset, :class => "btn btn-sm btn-warning " %>
        </div>
      <% end %>
      <div class="col-lg-2">
      <%= link_to 'Atras',roles_path, :class => "btn btn-sm btn-success" %>                          
      </div>
  </div>
  
<% end %>
  </div>