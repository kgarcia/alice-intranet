<%= form_for(patologia) do |f| %>
  <% if patologia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patologia.errors.count, "error") %> prohibited this patologia from being saved:</h2>

      <ul>
      <% patologia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="field">
    <%= f.label :TipoPatologia_id %>
    <%= f.text_field :TipoPatologia_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
