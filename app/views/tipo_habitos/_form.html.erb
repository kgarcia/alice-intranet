<%= form_for(tipo_habito) do |f| %>
  <% if tipo_habito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_habito.errors.count, "error") %> prohibited this tipo_habito from being saved:</h2>

      <ul>
      <% tipo_habito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
