<%= form_for(cirugia) do |f| %>
  <% if cirugia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cirugia.errors.count, "error") %> prohibited this cirugia from being saved:</h2>

      <ul>
      <% cirugia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="field">
    <%= f.label :TipoCirugia_id %>
    <%= f.text_field :TipoCirugia_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
