    <div class="loginColumns animated fadeInDown">
        <div class="row">

            <div class="col-md-6" style="color: white;">
                <h2 class="font-bold">Bienvenido a</h2>

                <%= image_tag 'alice-horizontal1.png', class: "img-responsive" %>

                <p>
                    Perfectamente diseñado y preparado con precisión para la administración y toma de decisiones gracias a las más de 50 funciones que te brinda Alice.
                </p>
     

            </div>
            <div class="col-md-6">
                <div class="ibox-content">
                <h2><%= t('.sign_in') %></h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "m-t") do |f| %>
                    
                        <div class="form-group">
                            <%= f.email_field :email, autofocus: true , class: "form-control", placeholder: "usuario@ejemplo.com" %>
                        </div>
                        <div class="form-group">
                        <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "" %>
                        </div>
                        <%= f.submit t('.sign_in') , class: "btn btn-primary block full-width m-b" %>
<% end %>
<%= render "devise/shared/links" %>

                       
                       
                    </form>
                    <p class="m-t">
                        <small>Unidad Quirurgica "La Trinidad"</small>
                    </p>
                </div>
            </div>
        </div>
        <hr />
    </div>


