<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
      <h2 class="text-dark-blue"><strong><%= @rango_edades.new.class.titulo %></strong></h2>
      <ol class="breadcrumb">
        <li>
          <a href="<%= url_for :controller => 'home', :action => 'index' %>">Inicio</a>
        </li>
        <li class="active">
            <strong class="text-blue">Listado</strong>
        </li>
    </ol>
  </div>
</div>
<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-sm-12">
      <div class="ibox float-e-margins">
        <div class="ibox-title">
          <h4 class="text-dark-blue"><strong>Listado: <%= @rango_edades.new.class.titulo %></strong></h4>
        </div>
        <div class="ibox-content">
          <%= link_to "Registrar", new_polymorphic_url(@rango_edades.new), :class => "btn btn-primary btn-sm", style: 'position: absolute; z-index: 9999; margin-top: 10px;'%> <div class="ibox float-e-margins">
          <table class="footable table table-stripped" data-sorting="true" data-filtering="true">
              <thead>
                <tr>
                  <th data-sorted="true">Descripción</th>
                  <th>Edad inicial</th>
                  <th>Edad final</th>
                  <th colspan="1"></th>
                </tr>
              </thead>

              <tbody>
                <% @rango_edades.each do |rango_edad| %>
                  <tr>
                    <td><%= link_to rango_edad.descripcion, edit_polymorphic_url(rango_edad) %></td>
                    <td><%= rango_edad.edad_inicial %></td>
                    <td><%= rango_edad.edad_final %></td>
                    <td>
                      <%= link_to 'Editar', edit_polymorphic_url(rango_edad), :class => "btn btn-warning btn-sm" %>
                      <%= link_to 'Eliminar', polymorphic_url(rango_edad), :method => :delete, data: { confirm: '¿Está seguro que desea eliminar el rango de edades?' } , :class => "btn btn-danger btn-sm" %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

