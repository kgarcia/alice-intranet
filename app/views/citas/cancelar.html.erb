<%= csrf_meta_tag %>
<p id="notice"><%= notice %></p>
<div class="ibox float-e-margins">
  <div class="ibox-title">
    <h2 class="parametros-titulo"><%= @citas.new.class.titulo %></h2>
  </div>
</div>
<div class="ibox-content">
  <table class="footable table table-stripped" data-sorting="true" data-filtering="true">
    <thead>
    <tr>
      <th data-breakpoints="xs" data-sorted="true">Cita #</th>
      <th>Servicio</th>
      <th>Especialista</th>
      <th>Paciente</th>
      <th colspan="1"></th>
      <th colspan="1"></th>
    </tr>
    </thead>
    <tbody>
    <% @citas.each do |cita| %>
        <tr>
          <td><%= cita.id%></td>
          <td><%= cita.turno.horario.servicio.tipo_servicio.descripcion %></td>
          <td><%= cita.turno.horario.servicio.especialista.nombre %> </td>
          <td><%= cita.persona.nombre %> <%= cita.persona.apellido%></td>
          <td>
            <%= link_to '', root_path+'cancelar_cita/'+cita.id.to_s, :class => "btn btn-danger btn-sm fa fa-times" %>
        </tr>

    <% end %>
    </tbody>
    <tfoot>
    <tr>
      <td colspan="5">
        <ul class="pagination pull-right"></ul>
      </td>
    </tr>
    </tfoot>
  </table>
</div>
<br>


<script type="text/javascript">
  $(document).ready(function(){
    $('.table').footable();
  });

</script>


