<%= form_for(lesion) do |f| %>
  <% if lesion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesion.errors.count, "error") %> prohibited this lesion from being saved:</h2>

      <ul>
      <% lesion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="field">
    <%= f.label :TipoLesion_id %>
    <%= f.text_field :TipoLesion_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
