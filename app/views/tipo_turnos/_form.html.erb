<%= form_for(tipo_turno) do |f| %>
  <% if tipo_turno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_turno.errors.count, "error") %> prohibited this tipo_turno from being saved:</h2>

      <ul>
      <% tipo_turno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
