<%= form_for(tipo_turno) do |f| %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion, :required => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
