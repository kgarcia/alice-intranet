<%= form_for(suscriptor) do |f| %>
  <% if suscriptor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suscriptor.errors.count, "error") %> prohibited this suscriptor from being saved:</h2>

      <ul>
      <% suscriptor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :persona_id %>
    <%= f.text_field :persona_id %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
