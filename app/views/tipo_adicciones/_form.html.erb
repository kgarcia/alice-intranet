<%= form_for(tipo_adiccion) do |f| %>
  <% if tipo_adiccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_adiccion.errors.count, "error") %> prohibited this tipo_adiccion from being saved:</h2>

      <ul>
      <% tipo_adiccion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
