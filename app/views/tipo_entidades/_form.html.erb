<%= form_for(tipo_entidad) do |f| %>
  <% if tipo_entidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_entidad.errors.count, "error") %> prohibited this tipo_entidad from being saved:</h2>

      <ul>
      <% tipo_entidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
