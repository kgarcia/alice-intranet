<%= form_for(criterio) do |f| %>
  <% if criterio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(criterio.errors.count, "error") %> prohibited this criterio from being saved:</h2>

      <ul>
      <% criterio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.number_field :estatus %>
  </div>

  <div class="field">
    <%= f.label :tipo_criterio_id %>
    <%= f.text_field :tipo_criterio_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
